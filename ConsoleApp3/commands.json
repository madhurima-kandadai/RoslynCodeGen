{
    "commands": {
        "test": {
            "fileName": "cmd.exe",
            "workingDirectory": ".",
            "arguments": "/c cd src\\ConsoleApp3 && scriptcs -install && scriptcs sample.csx"
        }
    },
    "-vs-binding": { "AfterBuild": [ "test", test ] }
}